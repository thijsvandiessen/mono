{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**"],
      "dotEnv": [".env"]
    },
    "analyze": {
      "outputs": [".next/**", "!.next/cache/**"],
      "dotEnv": [".env"]
    },
    "oxlint": {},
    "lint": {},
    "test": {}
  },
  "remoteCache": {
    "signature": false
  },
  "globalDependencies": ["tsconfig.json"]
}
